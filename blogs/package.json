{
  "name": "hwan-blog",
  "version": "2.0.0",
  "description": "Hwan Heo's blog with static site generation and enhanced markdown editor",
  "scripts": {
    "build": "node build-static.js",
    "build:incremental": "node build-static.js --incremental",
    "dev": "npm run build && npx http-server dist -p 8080 -o -c-1",
    "deploy": "npm run build && npm run deploy:dist",
    "deploy:dist": "gh-pages -d dist --dotfiles",
    "edit": "node editor-server.js"
  },
  "dependencies": {
    "@tweenjs/tween.js": "25.0.0",
    "marked": "^12.0.0",
    "three": "0.150.1"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "http-server": "^14.1.1",
    "sharp": "^0.35.3"
  }
}
