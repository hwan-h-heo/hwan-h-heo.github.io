{
  "name": "hwan-h-heo-portfolio",
  "version": "1.0.0",
  "description": "Hwan Heo's portfolio site with integrated blog",
  "scripts": {
    "build": "npm --prefix blogs run build",
    "dev": "npm --prefix blogs run dev",
    "deploy": "npm --prefix blogs run deploy"
  }
}
